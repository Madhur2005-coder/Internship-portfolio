
# Task 05 - KNN Classifier

This task demonstrates K-Nearest Neighbors, usually called KNN.

KNN classifies a new data point by looking at the most similar nearby examples. In this example, the model predicts whether a student performance profile is `Needs Support`, `Average`, or `Strong`.

## Run

```bash
python knn_classifier.py
```
